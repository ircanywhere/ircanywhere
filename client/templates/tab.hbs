<div {{bind-attr id=view.tabId}} class="tab selected">
	<div class="messages">
		{{render 'overlaybar' this}}
		{{render 'messages'}}
		{{render 'input'}}
	</div>
	{{#if view.showUserList}}
		{{render 'userlist'}}
	{{/if}}
</div>