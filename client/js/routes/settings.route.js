/*App.Router.map(function() {
	this.route('settings', {path: '/settings'});
});

App.SettingsRoute = AppRoute.extend({
	renderTemplate: function() {
		return this.render('settings', {
			into: 'application',
			outlet: 'modal'
		});
	},

	title: function(controller, model) {
		return controller.get('title');
	}
});*/